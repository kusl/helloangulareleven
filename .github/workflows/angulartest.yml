name: Angular Test

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: yarn and ng test
      run: |
        yarn
        npm run ng test 
      env:
        CI: true
      working-directory: .
